/**
 * @description {{&summary}}
 */
{{#mergeParam}}
export function {{name}} ({{parameters}}): Promise<
  {{#hasCustomReturnType}}CustomReturnType<{{&responseTypes}}>{{/hasCustomReturnType}}
  {{^hasCustomReturnType}}{{&responseTypes}}{{/hasCustomReturnType}}> {
  {{#payloadIn}}
    const payloadIn = {{&payloadIn}}
  {{/payloadIn}}
  return request(
    {{#pathParams.length}}`{{{path}}}`{{/pathParams.length}}
    {{^pathParams.length}}"{{{path}}}"{{/pathParams.length}},
    { {{#parameters}}payload,{{/parameters}} {{#payloadIn}}payloadIn, {{/payloadIn}} {{#payloadInType}}payloadIn: "{{&payloadInType}}", {{/payloadInType}} method: '{{method}}' }
  )
}
{{/mergeParam}}
{{^mergeParam}}
export function {{name}} (payload: { {{parameters}} }): Promise<
  {{#hasCustomReturnType}}CustomReturnType<{{&responseTypes}}>{{/hasCustomReturnType}}
  {{^hasCustomReturnType}}{{&responseTypes}}{{/hasCustomReturnType}}> {
  {{#pathParams.length}}
  const path = payload.path
  {{/pathParams.length}}
  return request(
    {{#pathParams.length}}`{{{path}}}`{{/pathParams.length}}
    {{^pathParams.length}}"{{{path}}}"{{/pathParams.length}},
    { ...payload, method: '{{method}}' })
}
{{/mergeParam}}