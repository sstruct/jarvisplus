import { default as requestFactory } from "@terminus/jarvisplus-runtime/lib/{{RequestFactoryName}}";

{{#customAgent}}
import { default as customAgent, onResponse, onError } from '{{&customAgent}}'

const request = requestFactory("{{&baseUrl}}", { request: customAgent, onResponse, onError });
{{/customAgent}}

{{^customAgent}}
const request = requestFactory("{{&baseUrl}}", {});
{{/customAgent}}

{{#hasCustomReturnType}}
  import type { CustomReturnType } from '{{&customAgent}}'
{{/hasCustomReturnType}}
